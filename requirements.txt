# Core. Everything needed to run the reconciler and the tests.
pydantic==2.13.5      # record + proof schemas, strict int validation on money fields
rich==15.0.0          # scorecard tables on stdout
pytest==9.1.1         # the verifier tests are the pitch

# The dashboard. Not needed for the CLI run or the tests.
fastapi==0.141.1
uvicorn==0.52.4
